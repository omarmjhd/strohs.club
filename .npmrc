# This is a personal, public project. Pin the public registry so the lockfile never
# picks up an internal mirror from a work machine's global npm config — CI has no
# credentials for one and `npm ci` fails with E401.
registry=https://registry.npmjs.org/
