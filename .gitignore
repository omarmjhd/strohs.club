node_modules/
.env
.env.*
.vscode/
.idea/
.claude/settings.local.json
dist/
.astro/
build/
.agent/
.DS_Store
*.log

# Generated one-pagers. PDFs embed a creation timestamp, so they are byte-different
# on every run even when the content is identical. PNGs and the Marp deck are
# deterministic and stay committed.
public/downloads/*.pdf
